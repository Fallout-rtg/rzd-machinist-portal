# Демо-портал машиниста: Путь к профессии

Информационный одностраничный веб-портал, разработанный для привлечения внимания к профессии машиниста и предоставления ключевой информации о локомотивах, условиях труда и образовании в сфере РЖД.

## 🚀 Технологии

Проект состоит из статического фронтенда и отдельной серверной функции для обработки обратной связи.

| Категория | Технология | Примечание |
| :--- | :--- | :--- |
| **Frontend** | HTML5 | Структура портала. |
| **Стили** | Tailwind CSS (CDN) | Быстрая и современная утилитарная CSS-библиотека. |
| **Скрипты** | Vanilla JavaScript | Вся интерактивность (карусель, модальные окна, форма). |
| **Backend/API** | Node.js (`bot.js`) | Серверless функция для обработки формы обратной связи. |
| **Интеграция** | Telegraf (Telegram Bot) | Отправка сообщений из формы прямо владельцу через Telegram. |

## ✨ Основные возможности

* **Интерактивный 3D-Карусель:** Динамическая галерея локомотивов с эффектом "объема" и отображением подробных технических характеристик выбранной модели.
* **Адаптивный дизайн:** Полная поддержка мобильных устройств с оптимизацией отображения логотипа и навигации.
* **Форма обратной связи:** Модальное окно для отправки сообщений, с валидацией доменов почты (только Gmail/Yandex) и ограничением на количество/размер прикрепляемых файлов.
* **Telegram Fallback:** Предоставление ссылки на Telegram-бот для отправки больших файлов или большого количества документов.
* **Анимации скроллинга:** Использование `IntersectionObserver` для плавного появления (fade-in) секций при прокрутке.

## 📁 Структура проекта

├── images/
│   ├── locomotives/    # Изображения локомотивов для карусели
│   ├── rzd.jpg         # Логотип РЖД
│   └── telegram.jpg    # Логотип Telegram
├── index.html          # Основной фронтенд файл
├── bot.js              # Серверная функция для API (/api/feedback)
└── README.md           # Текущий файл

## 🛠️ Запуск

### 1. Frontend

Поскольку проект использует Tailwind CSS через CDN и не требует сборки, для запуска достаточно:

1.  Клонировать репозиторий.
2.  Открыть файл `index.html` в любом современном веб-браузере.

### 2. Backend (Форма обратной связи)

Функционал формы обратной связи реализован в виде Serverless Function (`bot.js`) и требует развертывания на платформе, такой как Vercel или AWS Lambda.

1.  **Токен Бота:** Получите токен вашего Telegram-бота (`BOT_TOKEN`).
2.  **ID Владельца:** Определите ваш `OWNER_ID` (Telegram ID).
3.  Разверните `bot.js` как API-маршрут `/api/feedback`.
4.  Убедитесь, что в коде `index.html` в функции `submitForm` указан правильный путь к API:

    ```javascript
    fetch('/api/feedback', { // или полный URL, если развертывание отличается
        method: 'POST',
        body: formData,
    })
    ```

## 🎨 Стилизация

В проекте используется встроенный блок `<style>` и CDN-версия Tailwind CSS.

* **Основной цвет РЖД (Красный):** `--rzd-red: #D52B1E`
* **Шрифт:** Интер (Inter)
* **Фон:** `--bg-light: #F0F4F8` (Светло-серый/голубой)
